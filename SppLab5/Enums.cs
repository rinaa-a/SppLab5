namespace SppLab5
{
    public enum Lifetime
    {
        Singleton,
        Transient
    }

    public enum ServiceImplementations
    {
        None,
        First,
        Second,
        Third
    }
}
